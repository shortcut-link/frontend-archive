import styled from 'styled-components';

import { WithTag } from 'lib/styled-components';

export const Container = styled(WithTag)`
  display: inherit;

  max-width: 65rem;
  width: 100%;
  padding: 1rem;
`;
